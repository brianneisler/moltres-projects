import * as Stylus from '../styles/constants'

const baseStyles = {
  card_deck: {
    flex: 1,
    flexWrap: 'nowrap',
    // margin balancing
    marginLeft: -Stylus.GUTTER
  }
}

export default baseStyles
