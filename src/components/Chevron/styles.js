import * as Stylus from '../styles/constants' 

const baseStyles = {
  chevron: {
    backgroundColor: Stylus.TRANSPARENT,
    height: Stylus.CHEVRON_SIZE,
    width: Stylus.CHEVRON_SIZE,
    resizeMode: 'contain',
    tintColor: Stylus.CHEVRON_COLOR
  }
}

export default baseStyles
