import * as Stylus from '../styles/constants'

const baseStyles = {
  divider: {
    height: Stylus.DIVIDER_SIZE,
    backgroundColor: Stylus.DIVIDER_COLOR,
    alignSelf: 'stretch',
    marginLeft: 0
  }
}

export default baseStyles
